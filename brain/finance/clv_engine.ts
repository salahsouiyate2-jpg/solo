import { createSpecialistEngine } from "../core/specialist_engine";

export const clvEngine = createSpecialistEngine("financial", "CLV", "opportunity", "Customer lifetime value opportunity reviewed.");

