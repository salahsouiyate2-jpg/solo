import { createSpecialistEngine } from "../core/specialist_engine";

export const seasonalEngine = createSpecialistEngine("seasonal", "Seasonality", "opportunity", "Seasonal timing opportunity reviewed.");

