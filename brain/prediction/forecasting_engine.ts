import { createSpecialistEngine } from "../core/specialist_engine";

export const forecastingEngine = createSpecialistEngine("prediction", "Forecasting", "objective", "Demand and revenue forecast reviewed.");

