import { createSpecialistEngine } from "../core/specialist_engine";

export const benchmarkEngine = createSpecialistEngine("competition", "Benchmarking", "diagnosis", "Industry benchmark gap reviewed.");

