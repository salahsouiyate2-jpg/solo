import { createSpecialistEngine } from "../core/specialist_engine";

export const northStarEngine = createSpecialistEngine("growth", "North Star Metric", "objective", "Primary growth metric reviewed.");

