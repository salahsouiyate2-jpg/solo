import { createSpecialistEngine } from "../core/specialist_engine";

export const riceEngine = createSpecialistEngine("priority", "RICE", "opportunity", "Reach, impact, confidence and effort reviewed.");

